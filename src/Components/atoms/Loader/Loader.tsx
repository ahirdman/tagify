import * as React from 'react';
import './Loader.scss';

const Loader = () => {
  return (
    <div className="container">
      <div className="loader"></div>
    </div>
  );
};

export default Loader;
